/*
import { createClient } from "redis"

export const cache = await (async ()=>{
        return await createClient().on("error", (err)=>{
            console.log("Failed to connect to redis: ", err)
        }).connect()
})()
*/
